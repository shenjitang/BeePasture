resource: 

    file1: 
        url: "file://szbinfo.txt?format=plantext"
        
gather:
  - url: "http://app.cnstock.com/api/rss/index/szkx?output=json&p=1"
    encoding: utf8
    xpath: json($)
    script: "${unicode2str(it)}"
    save: 
      to: infoList

#保存
persist:
    infoList: 
        resource: file1
